import React from 'react'
import ButtonContainer from '../styles/buttonStyle'

const Button = ({text}) => {
  return (
    <ButtonContainer>
       {text}
    </ButtonContainer>
  )
}

export default Button